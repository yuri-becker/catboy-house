---
- name: ⬆️ Pushes journald config
  ansible.builtin.copy:
    src: files/systemd/journald.conf
    dest: "/etc/systemd/journald.conf"
    mode: "0644"
    owner: "root"
    group: "root"
  become: true
