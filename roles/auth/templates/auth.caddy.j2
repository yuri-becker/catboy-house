{{ auth_lldap_domain }} {
  log
	import common-headers
  import forward-auth {{ docker_gateway }}:{{ auth_authelia_port }}
	reverse_proxy {{ docker_gateway }}:{{ auth_lldap_port }}
}

{{ auth_authelia_domain }} {
  log
	import common-headers
	reverse_proxy {{ docker_gateway }}:{{ auth_authelia_port }}
}
