[web]
type = "db"
db = "mysql"
host = "localhost"
dbname = "{{ icinga_web_db }}"
username = "{{ icinga_web_db }}"
password = "{{ icinga_web_db_password }}"
use_ssl = "0"

[icingadb]
type = "db"
db = "mysql"
host = "localhost"
dbname = "{{ icinga_icingadb_db }}"
username = "{{ icinga_icingadb_db }}"
password = "{{ icinga_icingadb_db_password }}"
charset = "utf8mb4"
use_ssl = "0"
