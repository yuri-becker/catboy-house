object Endpoint NodeName { }

object Zone ZoneName {
  endpoints = [ NodeName ]
}

object Endpoint "mantis" { }

object Zone "mantis" {
  parent = "master"
  endpoints = ["mantis"]
}

object Zone "global" {
  global = true
}
